from django.urls import path
from django.views.decorators.csrf import csrf_exempt

# from .views import LiveView, StatView, LiveJsonView, StatJsonView


urlpatterns = [
    #  path("live", LiveView.as_view(), name="live"),
    #  path("live-json", LiveJsonView.as_view(), name="live_json"),
    #  path("stat", StatView.as_view(), name="stat"),
    #  path("stat-json", StatJsonView.as_view(), name="stat_json"),
]
